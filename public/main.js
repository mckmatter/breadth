$(document).ready(function() {
	console.log('document ready');
	




	//writeGraph(1);
	/*
	function writeGraph(id) {
		console.log("writeGraph");

		var url = '/graph/' + id
		console.log(url)

		$.ajax({
			'type': 'GET',
			'url': url,
			statusCode: {
				200: function(res) {
					$(".content").empty();
					$(".content").append(res)
				}
			}
		})

	}
	*/

})